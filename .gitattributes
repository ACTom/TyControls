# Shell scripts must keep LF endings or they fail on macOS/Linux ("bad interpreter: bash\r").
*.sh   text eol=lf

# PowerShell scripts are Windows-side; CRLF is fine and avoids editor churn.
*.ps1  text eol=crlf
